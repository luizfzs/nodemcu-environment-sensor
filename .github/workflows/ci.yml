name: Arduino CI

on: 
  push:
    branches: 
      - master
  pull_request:
    branches: 
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/arduino-build@v1
    - name: pull arduino-cli image
      run: docker pull arduino/arduino-cli
